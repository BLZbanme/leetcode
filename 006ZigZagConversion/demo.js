var convert = function(s, numRows) {
    var tmpArray = [];
    for(let i = 0; i < numRows; i++){
        tmpArray.push([]);
    }
    var charArray = s.splite('');
    for(let i = 0; i < charArray.length; i++){
        
    }
};